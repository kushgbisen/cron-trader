# cron-trader configuration

telegram:
  enabled: true
  ping_on_run: true  # Always send message when CI runs

# Risk per trade in USD (for P&L calculation)
default_risk_usd: 500

# Strategies to run
strategies:
  h4_breakout_ensemble:
    enabled: true
    risk_usd: 500
    
  weekly_breakout:
    enabled: true
    risk_usd: 500
  
  vol_squeeze_sol:
    enabled: true
    risk_usd: 500
  
  btc_leadlag_eth:
    enabled: true
    risk_usd: 500
